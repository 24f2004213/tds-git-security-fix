name: DevSync 24f2004213@ds.study.iitm.ac.in
run-name: Daily Update by 24f2004213@ds.study.iitm.ac.in
on:
  push:
  schedule:
    - cron: '15 12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  daily_commit:
    name: Job for 24f2004213@ds.study.iitm.ac.in
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 24f2004213@ds.study.iitm.ac.in
        run: |
          git config --global user.name "DevSync Automation"
          git config --global user.email "24f2004213@ds.study.iitm.ac.in"
          echo "Activity log for: $(date)" > daily_log.txt
          git add daily_log.txt
          git commit -m "chore: update activity log for 24f2004213@ds.study.iitm.ac.in" || echo "No changes"
          git push
