name: CI Cache Speedup
on: 
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Cache
        id: cache-step
        uses: actions/cache@v4
        with:
          path: .
          key: cache-e64fe29

      - name: prime-cache-e64fe29
        run: echo "Finalizing step for portal verification"
